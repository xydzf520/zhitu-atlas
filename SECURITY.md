# 安全问题报告

本机应用会处理简历、对话、招聘账号会话和模型凭据。公开 Issue 只包含版本、入口、任务阶段与脱敏复现步骤。

涉及凭据泄漏、跨账号读取或未经授权发送时，请使用托管仓库的私密漏洞报告入口（若维护者已启用）。入口不可用时，先提交不含漏洞细节的联络请求，由维护者提供私密渠道；不要公开可利用细节、Cookie、简历或对话。

当前维护主分支。安全修复随新版发布，旧版本是否需要迁移会在更新日志说明；没有固定响应时限承诺。

## 现有边界

- HTTP 仅监听回环地址，校验来源和本机会话令牌。
- 模型配置保存在仓库外本机私有目录，不回传密钥，不随迁移备份导出。
- 兼容接口必须由用户在设置明确填写。密钥绑定具体地址，更换地址不会复用另一地址的密钥，禁止跟随重定向。
- 模型工具为固定的只读能力；模型不能修改发送权限或执行任意代码。
- 导入、JD、公开网页和消息都是不可信数据，不能覆盖系统规则。

这些措施不代表已通过独立安全审计。数据流、文件权限和用户操作边界见 [隐私说明](docs/PRIVACY.md)。
