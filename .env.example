# Optional: an absolute LOCAL data directory OUTSIDE the source checkout.
# The application uses ~/.local/share/zhitu-atlas by default.
# Export this variable in your shell; Atlas does not load credentials from .env.
# ATLAS_DATA_ROOT=/absolute/path/outside-repository/atlas-data
# Configure model credentials in Settings. Do not add keys, passwords or Cookies here.
