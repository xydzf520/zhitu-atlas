# 职途 Atlas 0.19.1 修复验收

日期：2026-09-21。针对 [0.19.0 系统评审](REVIEW_019_SYSTEM_20260921.md) 中已复现的七类问题，完成代码修复、回归和本机客户端更新。

## 修复结果

| 问题 | 当前行为 | 验证 |
| --- | --- | --- |
| 学历替代条件误排 | “本科或硕士”按可接受分支判断；“硕士及以上”仍要求硕士；独立必要条件、优先项与学习形式分别核对 | 本科满足 OR、本科不满足硕士要求、未确认学历及未知替代条件保持待核实 |
| 同句多个年限漏核 | 产品经验及团队管理年限拆成独立要求，各自关联已确认事实；不以总工龄替代专业或管理年限 | 产品满足但管理未知、管理不足、管理已确认、优先管理条件、复杂替代关系 |
| 跨账号机会操作 | 统一归属检查，详情、候选、合并、关联记录、归档、草稿与汇总隔离账号；无归属平台数据不归入当前账号 | 拒绝跨账号读写；历史错误合并不带出另一账号的关联内容；同账号合并与撤销正常；同一招聘者关联多个岗位仍只显示一次 |
| 无关 Agent 编辑使任务过期 | 任务声明实际依赖的 Agent；沟通话术不依赖市场研判及扩展搜索词；跟进变体使用跟进 Agent | 无关修改后复用原任务并正常执行；真实依赖变化时执行前中断，未重复调用 |
| 仓库核验时效拖累岗位分析 | 分开稳定项目经历与公开链接验证；分析输入不包含未经当前核验的链接／许可证声明；发送继续使用有时效的公开状态 | 核验过期后 JD 缓存命中，未增加生成调用；招呼版本变化，旧公开链接不可沿用；项目经历变化仍失效 |
| 城市与区县冲突 | 城市字段与唯一可定位区县交叉核验；冲突待核实；重名区县与道路名称不强行推定 | 上海＋海淀区冲突、朝阳区保持歧义、北京路不识别为北京；限定 1,024 项地区结果缓存 |
| 导入分析统计缺失 | 手动／文件机会使用与机会发现一致的 `local:<机会ID>` 获取分析；账号、资料、岗位和模型版本均校验 | 两条有效导入分析计为两条；旧结果及其他账号记录不计入 |

复杂、无法可靠拆解的资历表达继续待核实，不承诺规则解析覆盖全部自然语言。学历只用于既有匹配事实，不自动写入对外简历。

## 验证记录

- 新增 `tests/system-review-fixes.test.cjs`，15 项针对性回归。
- 全量 **325/325** 通过，包括原有数据迁移、备份损坏、恢复、发送幂等与负载用例。
- 桌面浏览器原生隔离检查 **47/47** 通过，覆盖连接、同步、联系与发现流程。使用本地平台协议夹具，不使用真实招聘者充当测试对象。
- Node、Vue 类型检查通过；本机网页与 Electron 生产构建通过。
- 5,000 岗位聚合测试通过。首次修复后地区交叉核验使该测试变慢，随后增加预编译区县匹配及有限缓存并通过完整复测。
- 模型相关缓存测试使用受控返回结果，验证生成调用数及证据状态；本轮没有调用付费模型。
- 安装 0.19.1 后，实际本机服务的全局分析、机会发现、机会汇总及任务中心查询正常；网页更新日志显示 v0.19.1，任务中心显示全部自动任务暂停。
- 业务数据备份在隔离目录恢复，SQLite 完整性检查通过，数据数量一致；原目录未被覆盖。

本机证据位于 `artifacts/fixes-0191-20260921/`：`check.log`、`build-release.log`、`native/*.json`、`data-verification.json`、`restore-verification.json`、`live-verification.json`。该目录包含私人备份，仅本地保存，不用于开源发布。

## 数据与发布

升级前后校验一致：107 个岗位、11 个会话、41 条消息、16 个资料版本、11 个跟进机会、1 个联系任务、0 次发送尝试。相关表内容哈希一致；个人资料、求职策略、模型配置哈希一致。发送与全部自动任务保持暂停，未改变既有城市、薪资和额度策略。

此次分析输入契约升级，旧 JD 分析和两份全局研判保留可读，按新依据标记过期。不会自动发起付费重算，不会把旧结果直接用于发送。排队任务的旧版本依据也不会被静默升级或重放。

安装清单哈希与包哈希保存在 `package-delivery.json`，安装结果记录在 `install-delivery.json`。当前发布目录为 `~/.local/share/zhitu-atlas-app/releases/2a9ba188566f8b7a`，上一版为 `af483c3fa56a2a35`。数据库结构版本不变。需回退时先退出客户端，使用校验安装器切回上一版，保留当前业务库；若需恢复升级前的数据状态，先另外备份当前数据，再使用 `preinstall/data`，不要运行中覆盖数据库。浏览器登录资料使用原有目录，本轮未重置。

## 尚未计入完成的范围

本轮解决七类可复现缺陷及相邻账号访问入口。评审中的今日行动直达、简历公司—任职—项目进一步结构化、AI 分层预算、长时间运行验收等后续产品改进没有因此自动视为完成。真实 BOSS 发送仍需使用本人确实准备联系的合适岗位独立验收；此次零真实发送。
