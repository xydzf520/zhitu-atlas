# 0.21.1 代码来源与说明核查

> 本页保留重写前的核查结果。后续工程配置、路由和历史导入器重写见 [0.21.2 记录](REWRITE_0212_20260922.md)；下方 8 个相同配置不是当前版本的结论。

日期：2026-09-22。核查当前工作树、包配置、入口依赖图及本机保存的上游基线 `c4ebdb0882d8dbecfd9864f980f6d13bd1eb4b2a`，不运行招聘平台操作。

## 结论

**旧业务执行模块已移除，历史来源、通用工程配置和资料兼容仍然存在。** “没有旧执行链”不等于“所有文件均从零编写”或“任何地方都不出现旧名字”。

| 检查对象 | 实际结果 |
| --- | --- |
| 应用工作区 | 当前仅 `packages/ui`；旧开聊、调度、SQLite 插件、登录扩展等工作区不在当前树中 |
| 依赖和锁文件 | 未找到旧工作区依赖；Puppeteer 是根项目的开发验证依赖，客户端平台操作使用 Electron |
| 桌面入口依赖图 | 77 个输入文件，全部来自 `packages/ui/src`；外部依赖为 Electron、Node 内置模块及 fflate |
| 后台入口依赖图 | 67 个输入文件，全部来自 `packages/ui/src`；外部依赖为 Node 内置模块及 fflate |
| 预加载入口依赖图 | 2 个输入文件，全部来自 `packages/ui/src`；外部依赖为 Electron |
| 同文件内容比对 | 上游基线 268 个文件与核查前当前 287 个文件比较，完全相同的 8 个均为工程配置，清单见 [来源说明](PROVENANCE.md) |
| 业务源码片段比对 | 当前 `src` 下 132 个 TS／JS／Vue 文件与上游对应类型文件比较，未发现连续 12 个非空行、规范化后超过 240 字符的相同片段 |
| 前端路由 | 旧页面名称只重定向到 Atlas 页面，不导入旧页面组件 |
| 品牌和发行配置 | 应用名称、appId、入口及发布包使用 Atlas；原来的图标与上游下载入口不作为当前界面资源 |

各入口共享模块，77、67、2 不能相加当作独立文件总数。完全相同文件和连续行检测只能发现直接复制，不能证明不存在较短片段、改写或产品思路继承，因此不据此宣称“100% 原创”。依赖图检查覆盖静态构建输入，不能替代对运行时生成脚本的代码评审。

## 发现并修正的说明

- 根 README 仍重复强调早期改造过程，改为聚焦当前产品，开发者可跳转查看简短来源说明。
- 上游 README 全文仍包含旧下载链接、个人面试评论、群聊、旧风险声明及系统操作命令，已从当前文档移除；原文件仅留兼容链接入口，早期版本可从 Git 历史追溯。
- 桌面子项目 README 仍写 `0.18.0`，改为引用实际包版本，避免继续漂移。
- 架构页仍标注适用 `0.19.0`，已按当前 `0.21.1` 更新说明；功能引入版本保留。
- 文档索引将早期审查写成“本轮”且混入当前指南，已按当前说明与历史验收分开，保留历史测试与恢复证据。

## 保留的兼容与来源

Atlas 自己实现的首次目录迁移、旧库只读导入、旧筛选条件兼容、旧网址跳转继续保留。删除它们会影响已有用户的资料和书签，不能为了名称搜索结果归零而删去。

历史验收里的旧路径、上游名称、版本和哈希不改写为当前事实。旧 `public.db` 使用只读连接迁入 Atlas；旧授权不直接恢复为已发送。BOSS 平台 `/web/geek/` 是平台地址，与旧软件实现不同。

上游来源、现有许可声明及第三方组件署名保留。本次没有重写 Git 历史或删除恢复资料。

## 持续检查与验收

新增 `pnpm check:runtime`，读取桌面、后台和预加载的实际构建依赖图，检查应用工作区、输入文件和外部模块。它已接入生产构建；旧模块即使被构建器标为外部依赖，也会被检查。

新增回归覆盖当前三个入口、旧包被外部化、旧跟进源码与兼容字符串的区别。`pnpm check` 已通过：**464 项回归，0 失败、0 跳过**；Node／Vue 类型检查、网页和桌面生产构建、入口依赖检查及发布资源扫描均通过。另核对 41 份 Markdown 中的 146 个本地文件链接，没有缺失目标。

本次不改变求职策略、模型配置或发送授权，不升级运行中的客户端，也不发送 BOSS 消息。
