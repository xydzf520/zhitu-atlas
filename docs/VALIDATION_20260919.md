# 独立项目验证记录

> **历史版本记录**：正文只描述文内日期和版本。功能、测试数、路径及回滚命令不代表当前版本；现行操作见 [使用指南](USER_GUIDE.md) 和 [运行与升级](OPERATIONS.md)。原有测试结论保留，不追认为本轮复测。

验证日期：2026-09-19。项目目录：`$HOME/work/zhitu-atlas`。

| 检查 | 结果 |
| --- | --- |
| Node.js 回归测试 | 210 项通过，失败 0 项 |
| TypeScript Node 与 Vue 检查 | 通过 |
| SQLite 插件构建 | 通过 |
| 正式网页构建 | 通过 |
| Electron 主进程、预加载及界面构建 | 通过 |
| pnpm 8.15.9 冻结锁文件离线检查 | 通过 |
| Agent 管理隔离界面检查 | 8 项通过，覆盖两种窗口尺寸、草稿保护、跨窗口冲突、启停、历史和提示词快照 |
| 暂存内容检查 | 未包含运行数据库、简历附件、Cookie、用户 API 密钥或旧 Git 备份 |

命令从新项目根目录执行，依赖使用本机已经安装的版本。本轮没有在全新机器上重新安装 Electron 和全部原生依赖。锁文件校验已通过；安装解析仍提示既有 Pinia 3.0.2 与 Vue 3.4.15 的 peer 版本范围差异，本轮未扩展到框架升级。

构建保留 bundle 超过 500 kB 的体积提示。界面测试使用隔离资料和模拟模型，未向招聘者发送消息。平台真实发送的既有验收范围保持不变。

本机日志位于 `artifacts/migration-20260919` 和 `artifacts/agents`，不提交运行产物。原有 2026-09-18 Agent 发布已进行过一次 DeepSeek 官方接口测试；本轮目录迁移未重复产生付费模型调用。
