# 全国机会地图修复 · 0.19.2

2026-09-21，已安装并启动。

## 变更

- 全景总览与企业地图始终使用 Leaflet 地图，取消默认地区条形图。
- 默认取已保存的首选求职城市；当前资料为上海。支持全国及 373 个城市／地区选项，城市即使没有岗位也可查看。全国按城市汇总，下钻城市后展示区域汇总和 WGS84 确认位置。
- 保留阶段颜色、岗位弹窗、区县筛选和地址列表，后台无变化读取不会关闭弹窗；全国汇总不与同城精确标记叠加。
- 瓦片失败保留本地区域轮廓，不退成条形图，不批量缓存瓦片。地区中心仅用于地图展示，不写入企业坐标或通勤起点。

## 验证

- 回归测试 **331/331**；类型检查通过；网页与 Electron 生产构建通过。
- 隔离数据验证上海、北京、全国下钻、成都空数据、未知地点、区域与精确坐标、企业页联动、弹窗稳定；模拟街道瓦片 404 后 72 个本地轮廓仍可用。
- 1280 像素窗口无横向溢出。浏览器视口覆盖未改变实际宽度，因此不宣称独立完成 1920 像素验收。
- 已安装真实页面默认上海：11 个机会分布在 6 个区；全国视图汇总为上海 11 个。当前 11 个均为区域定位，没有已确认的企业精确坐标。
- 升级前后：107 个岗位、11 个会话、41 条消息、16 个资料版本、11 个机会及相关业务记录哈希一致。模型配置、策略、发送额度与暂停状态一致，未调用模型或发送消息。

证据：`artifacts/map-0192-20260921/` 的 tests.log、types.log、build.log、ui-verification.json、before-install.json、after-install.json、live-verification.json。

## 发布与回滚

- 当前发布目录：`$HOME/.local/share/zhitu-atlas-app/releases/fbfce1557bdaab2d`。
- 发布清单 SHA-256：`fbfce1557bdaab2d613aeb22ce8fd8c116ff23cea58b622220ca59a8413183b5`。
- 前版：`$HOME/.local/share/zhitu-atlas-app/releases/2a9ba188566f8b7a`（0.19.1），升级器保留；本次没有数据库结构变化。
- 退出客户端后，可使用既有校验安装器切回 0.19.1 发布包。升级前本机数据与浏览器备份位于私有 `artifacts/map-0192-20260921/preinstall/`，勿提交仓库或分享。

## 地理精度

详见 [地图来源](MAP_DATA_SOURCES.md)。6 个城市暂无可靠城市参考点，明确提示使用所属省区视图；离线轮廓不含街道。全国视图并不表示存在全国岗位样本。
