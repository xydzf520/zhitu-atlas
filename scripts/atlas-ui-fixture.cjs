const {buildSync}=require('esbuild'),fs=require('node:fs'),os=require('node:os'),path=require('node:path');
const root=fs.mkdtempSync(path.join(os.tmpdir(),'atlas-ui-'));process.env.ATLAS_DATA_ROOT=root;const file=path.join(root,'server.cjs');buildSync({stdin:{resolveDir:process.cwd(),contents:"export * from './packages/ui/src/main/features/atlas-server';export * from './packages/ui/src/main/features/atlas-service';export * from './packages/ui/src/main/features/atlas-store';"},bundle:true,platform:'node',format:'cjs',outfile:file});const a=require(file),h=a.createAtlasHandlers(async()=>({data:{applications:[],databaseAvailable:true}}));const s=h.get('career-workspace-snapshot')();s.state.profile={name:'隔离验收用户',headline:'AI 产品负责人',summary:'用于本地交互验收的虚构资料。',targetRoles:['AI产品负责人'],preferredCities:['上海'],minimumMonthlyK:40,evidence:[{id:'fixture',confirmed:false,title:'隔离项目',text:'负责企业工具产品建设',keywords:['AI'],source:'测试数据'}],resumeText:'隔离验收用户\n个人优势\n用于统一资料功能验收，不用于实际求职。'};s.state.opportunities=Array.from({length:55},(_,i)=>({id:'test-'+i,job:{jobName:'AI产品负责人',companyName:'隔离企业 '+(i+1),description:'负责企业产品规划、需求分析和落地交付。'.repeat(10),address:'上海浦东新区',salaryLow:40,salaryHigh:60},stage:'待评估',nextDate:'',note:''}));h.get('career-workspace-save')({},{state:JSON.stringify(s.state),baseRevision:s.revision});a.startAtlasServer(path.resolve('packages/ui/web-dist'),5179).then(server=>{console.log('UI fixture ready: http://127.0.0.1:5179/desktop.html');process.on('SIGTERM',()=>server.close(()=>{a.closeAtlas();fs.rmSync(root,{recursive:true,force:true});process.exit(0)}))})
